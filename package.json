{
  "name": "vela",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Simulatore di navigazione a vela nel Mar Ionio",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "test:golden": "node --test test/golden.test.js",
    "charts": "python3 tools/build-charts/build.py tools/build-charts/ionian.yaml"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
